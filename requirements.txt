# Financial Analysis Skill — Dependencies
# Compatible with Python 3.9+
#
# Core data fetching
# yfinance floor pinned to a MODERN, verified schema: the fetchers here assume
# aggregated .recommendations (period/strongBuy/...), .earnings_history, and
# percent-valued `dividendYield` (bugs #8/#10/#14). Older releases exposed
# `dividendYield` as a fraction and legacy recommendation/earnings schemas —
# the code is already incompatible with those, so the declared floor must say so.
yfinance>=1.5.1
requests>=2.31.0
feedparser>=6.0.11

# Data analysis (pandas 2.0.x is the last line supporting 3.9)
pandas>=1.5.0
numpy>=1.24.0

# Technical analysis
# pandas-ta: installed separately by setup.sh (may fail on older Python)
# tradingview-ta: TradingView consensus ratings (26 indicators)
tradingview-ta>=3.3.0

# Date/time handling
python-dateutil>=2.8.2
